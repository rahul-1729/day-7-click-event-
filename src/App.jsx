// click-event = An interaction when a user clicks on a specific element.We can respond t clicks by passing a caallback to the onClick event handler

import Button from './button.jsx'

function App() {
 
  return (<>
  <Button/>
  </>)
}

export default App
